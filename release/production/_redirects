# Cloudflare Pages — redirects/rewrites for Kids Creative Studio
# https://developers.cloudflare.com/pages/configuration/redirects/
#
# Kids Creative Studio uses HASH-based routing (all in-app navigation lives
# after the "#", e.g. /#/coloring). Every real URL therefore resolves to a
# file that already exists on disk, so NO SPA rewrite/fallback rule is
# required. This file is intentionally left with no active rules.
#
# If you ever switch the router to History/clean-URL mode, enable a
# single-page fallback by uncommenting the line below:
#
# /*    /index.html    200
