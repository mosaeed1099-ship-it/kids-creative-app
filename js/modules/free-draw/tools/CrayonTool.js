/** CrayonTool — grainy, waxy texture. */
import BaseBrushTool from './BaseBrushTool.js';
export default class CrayonTool extends BaseBrushTool {
  constructor(app) { super(app, 'crayon', 'crayon'); }
}
