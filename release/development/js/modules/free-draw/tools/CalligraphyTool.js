/** CalligraphyTool — angled nib whose width follows stroke direction. */
import BaseBrushTool from './BaseBrushTool.js';
export default class CalligraphyTool extends BaseBrushTool {
  constructor(app) { super(app, 'calligraphy', 'calligraphy'); }
}
